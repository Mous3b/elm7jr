import 'package:elm7jr/Features/DriverDetailsView/data/models/driver_pay_model.dart';
import 'package:flutter/material.dart';

class DriverPayCard extends StatelessWidget {
  const DriverPayCard({super.key, required this.model});
  final DriverPayModel model;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
